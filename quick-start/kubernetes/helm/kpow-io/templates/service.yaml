apiVersion: v1
kind: Service
metadata:
  name: {{ include "kpow-io.fullname" . }}
  labels:
    {{- include "kpow-io.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kpow-io.selectorLabels" . | nindent 4 }}
