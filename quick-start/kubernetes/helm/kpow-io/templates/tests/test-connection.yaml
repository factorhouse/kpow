apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kpow-io.fullname" . }}-test-connection"
  labels:
    {{- include "kpow-io.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kpow-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
